// If statement
let age = 19;

// If-else statement


// Else-if ladder
const grade = '200';
functioncalculateGrade(marks);
if (marks>=80) {
    return 'A';

}else if (marks>=70) {
    return 'B';

} else if (marks>=60) {
    return 'C';

} else if (marks>=50) {
    return 'D';

} else {
    return 'Umebant!';
}

console.log(functioncalculateGrade(161));

// Switch statement
function calculateGradeUsingSwitch(marks){
    switch (marks)
    {
        case 80:
            return 'A';
        case 70:
            return 'B';
        case 60:
            return 'C';
        case 50:
            return 'D';
        default:
            return 'Umebant!';
    }
}

// Ternary operator


// While loop


// Do-while loop


// For loop


// For...in loop (for objects)


// For...of loop (for arrays and iterables)


// Break statement to exit a loop early


// Continue statement to skip an iteration in a loop

